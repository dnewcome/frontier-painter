# Frontier Painter — Slice 01 in the dressed ship

This is the official **Slice 1** playthrough (draw handhold -> grab -> pull -> reach
goal -> win) shown in the now-dressed **clean-utilitarian spaceship interior**. The
gameplay beats (B1..B9) are unchanged from `slice-01-handhold`; only the room
dressing is new.

- Run label: `slice-01-dressed`
- Generated by: `RUN_LABEL=slice-01-dressed npm run playthrough`
- **Result: `goalReached === true`** (and verified to reproduce — see B9).

## Scene legend (one line)

Dressed ship — paneled walls, ceiling light strips, grated floor, and wall screens
frame the action; gameplay actors keep their colors: **blue ellipsoid** = player,
**amber tube** = drawn handhold, **green sphere** = goal (turns **gold** on win),
with the live HUD top-left and the "GOAL REACHED — WIN!" banner on victory.

## Artifacts

- Animated overview: [`demo.gif`](./demo.gif)
- Video: [`demo.mp4`](./demo.mp4) (h264, ~720p, 15 fps)
- Per-beat screenshots: [`frames/`](./frames) (1280×720 PNG) — B1 ready, B2 reset,
  B3 camera, B4 draw, B5 approach, B6 grab, B7 pull, B8 win, B9 replay.
- Raw recording: `video/*.webm`
